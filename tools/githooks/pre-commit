#!/bin/sh

TESTS=0

for xmlfile in $(
  git status -s \
  | sed -En "s/\s*[AM]\s*(.*\.xsl|.*\.xml|.*\.xhtml)/\1/p"
); do
  xmllint --noout --nonet "${xmlfile}"
  TESTS=$((${TESTS} + $?))
done

exit "$TESTS"
